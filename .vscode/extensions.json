{
  // The official Swift extension is the only one this setup needs: it drives SourceKit-LSP for code
  // intelligence and swift-format for format-on-save. See docs/development.md#editor.
  "recommendations": ["swiftlang.swift"]
}
